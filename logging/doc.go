// Package logging provides functions and classes that help create consistent log messages.
//
// Since resweave relies on the [zap.SugaredLogger] to generate logs using a json structure, this
// package can help ensure that your log messages are consistent and searchable.
package logging
